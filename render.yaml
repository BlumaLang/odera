services:
  - type: web
    name: soundwave-api
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: ./render-build.sh
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20
